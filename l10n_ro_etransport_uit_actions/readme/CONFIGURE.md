Nothing beyond the standard eTransport setup (ANAF access token and test/production environment on
the company). The scheduled action *RO e-Transport: UIT event status update* runs every 15 minutes
and can be adjusted under Settings → Technical → Scheduled Actions.
